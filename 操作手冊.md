# 台股券商分點量化分析系統操作手冊 (User Manual)

本手冊提供台灣證券交易所（TWSE）與櫃買中心（TPEX）全市場千萬筆券商分點資料之量化分析、主力雷達掃描、雲端自動化產製 Email 決策日報與轉檔工具之完整操作說明。

---

## 目錄 (Table of Contents)
1. [系統簡介與核心價值](#1-系統簡介與核心價值)
2. [專案目錄架構與模組清單](#2-專案目錄架構與模組清單)
3. [環境建置與依賴安裝](#3-環境建置與依賴安裝)
4. [組態設定與環境變數 (Config & Settings)](#4-組態設定與環境變數-config--settings)
5. [操作指令與使用範例](#5-操作指令與使用範例)
   - [5.1 本地執行主力波段連續重押雷達](#51-本地執行主力波段連續重押雷達)
   - [5.2 本地執行主力出貨/逃離雷達 (大戶下車反向偵測)](#52-本地執行主力出貨逃離雷達-大戶下車反向偵測)
   - [5.3 雲端全流程自動化日報 (Google Drive + Email HTML)](#53-雲端全流程自動化日報-google-drive--email-html)
   - [5.4 批次 Parquet 轉 Excel 工具 (多行程加速)](#54-批次-parquet-轉-excel-工具-多行程加速)
   - [5.5 尾盤放量站上 VWAP 與分點逆向歸因引擎](#55-本地執行尾盤放量站上均價線-vwap-與分點逆向歸因引擎-隔日沖波段大戶識別)
   - [5.6 外資與本土法人分點買賣超解密引擎](#56-本地執行外資與本土法人分點買賣超解密日報)
   - [5.7 myStock 雲端籌碼戰情室資料庫同步 (Supabase)](#57-mystock-雲端籌碼戰情室資料庫同步-supabase)
6. [GitHub Actions 雲端每日自動排程佈署](#6-github-actions-雲端每日自動排程與外部定時觸發)
7. [分析指標定義與報表解讀](#7-分析指標定義與報表解讀)
   - [7.1 主力波段連續重押雷達指標](#71-主力波段連續重押雷達指標)
   - [7.2 尾盤放量站上 VWAP 與分點逆向歸因指標](#72-尾盤放量站上-vwap-與分點逆向歸因指標)
8. [全市場股票名稱與市場別對照維護](#8-全市場股票名稱與市場別-上市上櫃興櫃-對照維護)
9. [常見問題排解 (FAQ & Troubleshooting)](#9-常見問題排解-faq--troubleshooting)

---

## 1. 系統簡介與核心價值

本系統為專門針對台股全市場千萬筆券商分點交易數據所建構之高敏捷量化分析引擎。

### 🌟 核心特色
1. **極速 DuckDB 分析引擎**：零拷貝直接穿透多天期 Parquet 巨量日報表，數秒內完成全市場統計運算。
2. **川湖（2059）+ 凱基-三多（9275）重押模型複製**：
   - 抓出「單一主力分點連續重押、買進純度超高、淨買超金額龐大」之黃金主力飆股。
3. **無伺服器 (Serverless) 雲端全自動化**：
   - 自動從 Google Drive 目標資料夾拉取最新 Parquet 數據。
   - 每日自動產生 FinTech 響應式 HTML 表格日報並發送至指定 Email 信箱，隨信夾帶 Excel 複盤明細。
4. **多行程批次轉檔引擎**：提供高效能 CPU 多核心平行轉換，秒級將 Parquet 大檔轉換為帶格式之 Excel (.xlsx)。

---

## 2. 專案目錄架構與模組清單

```
stock_data_analysis/
├── docs/
│   └── ai_planning/
│       └── cloud_daily_report_planning.md  # 雲端 Email 自動化架構規劃書
├── .github/
│   └── workflows/
│       └── daily_heavy_accumulation_report.yml # GitHub Actions 每日排程工作流
├── main_cloud_runner.py          # 雲端自動化日報全流程主控入口
├── cloud_gdrive_downloader.py    # Google Drive 近 N 日 Parquet 檔案智慧下載模組
├── cloud_report_generator.py     # 主力重押模型運算、HTML 模板渲染與 Excel 產製模組
├── send_email_report.py          # 安全 SMTP 郵件派發模組 (支援 HTML 與 Excel 附件)
├── find_similar_cases.py         # 本機主力重押雷達掃描工具
├── find_exit_cases.py            # 本機主力出貨/逃離雷達掃描工具 (find_similar_cases.py 反向指標)
├── reverse_broker_matcher.py     # 尾盤放量站上 VWAP 與分點逆向歸因引擎 (隔日沖/波段大戶識別)
├── institutional_broker_rankings.py # 外資主要席位與本土法人部買賣超解密引擎 (獨立日報)
├── sync_to_mystock.py            # myStock 雲端戰情室 Supabase 資料同步引擎 (全模組聚合)
├── supabase_schema.sql           # myStock 雲端資料庫完整建表 SQL 腳本
├── broker_persona_db.json        # 主力券商分點行為特徵庫 (隔日沖、波段外資等)
├── chip_intelligence_analysis.py # 進階籌碼情報模組 (出貨預警/對敲雜訊/集團同步等)
├── batch_parquet_to_excel.py     # 多行程 Parquet 批次轉 Excel 工具
├── parquet_to_excel.py           # 單檔 Parquet 快速轉檔工具
├── compare_datasets.py           # 資料庫一致性驗證與比對工具
├── broker_name_map.json          # 全市場券商分點中文名稱對照表
├── stock_name_map.json           # 全市場股票代號與繁體中文名稱快取
├── stock_market_map.json         # 全市場股票上市/上櫃/興櫃市場類別快取
├── update_stock_mappings.py      # 全市場股票名稱與市場類別對照資料庫建置/更新模組
├── requirements.txt              # 專案 Python 套件依賴清單
└── 操作手冊.md                   # 系統完整操作手冊
```

---

## 3. 環境建置與依賴安裝

### 3.1 Python 環境需求
* **Python 版本**：建議使用 Python 3.10 ~ 3.12。

### 3.2 安裝依賴套件
```bash
pip install -r requirements.txt
```

若需使用 Google Drive API 或發送郵件功能，請確保已安裝：
```bash
pip install duckdb pandas numpy openpyxl requests beautifulsoup4 google-api-python-client google-auth
```

---

## 4. 組態設定與環境變數 (Config & Settings)

若要啟用「Google Drive 雲端數據拉取」與「Email 自動寄送」，可在專案根目錄建立 `.env` 檔案或在 GitHub Repository Secrets 中配置：

```ini
# Google Drive 雲端同步
GDRIVE_UPLOAD_URL="https://script.google.com/macros/s/YOUR_GAS_DEPLOYMENT_ID/exec"
GDRIVE_FOLDER_ID="your_google_drive_folder_id"
GDRIVE_LOG_FOLDER_ID="your_google_drive_log_folder_id"

# Telegram 即時推播 (選用)
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"
TELEGRAM_CHAT_ID="your_telegram_chat_id"

# SMTP 郵件通知 (Gmail)
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your_email@gmail.com"
SMTP_PASSWORD="your_gmail_app_password"   # 請使用 Gmail 16 位應用程式專用密碼
RECEIVER_EMAIL="your_email@gmail.com"
```

---

## 5. 操作指令與使用範例

### 5.1 本地執行主力波段連續重押雷達

```bash
# 預設執行 (掃描 output 目錄下所有 Parquet 檔案)
python find_similar_cases.py

# 自訂門檻 (淨買超 >= 0.5 億元, 買進純度 >= 75%, 張數 >= 100 張)
python find_similar_cases.py --min-amount 0.5 --min-ratio 75 --min-vol 100
```

### 5.2 本地執行主力出貨/逃離雷達 (大戶下車反向偵測)

```bash
# 預設執行：長期基期 60 日內曾高純度重押、且近 5 日翻臉高純度賣出之組合
python find_exit_cases.py

# 指定查詢特定股票 (例: 2890 永豐金)
python find_exit_cases.py --symbol 2890 --min-long-amt 0 --min-recent-amt 0 --min-recent-ratio 0

# 自訂門檻：長期買進佔比 >= 80%、近期賣超 >= 0.5 億元
python find_exit_cases.py --min-long-ratio 80 --min-recent-amt 0.5
```

輸出欄位包含「出貨嚴重度(%)」(近期賣超金額佔原始吸籌金額之比例) 與「出貨損益(%)」(近期賣出均價相對原買進均價之損益)，並自動標記「💰 逢高獲利了結」或「🩸 認賠殺出」。

> ⚠️ **注意事項 (窗口邊界限制)**：「原吸籌起日」採用累計金額突破 `--ignition-ratio` (預設20%) 門檻之演算法偵測，但仍受限於 `--long-days` 所涵蓋的資料範圍（預設 60 個交易日）。若主力實際上早於此窗口起點就已開始布局，系統只能回溯到窗口內最早可見的資料日，並非該分點真正的第一次進場日。若需更完整的歷史起漲判斷，可調高 `--long-days`（例如設定為系統目前可用的最大交易日數）以涵蓋更長區間。

### 5.3 雲端全流程自動化日報 (Google Drive + Email HTML)

```bash
# 1. 完整全自動執行：從 GDrive 下載近 5 日數據 -> 分析 -> 產出 HTML 與 Excel -> 寄送 Email
python main_cloud_runner.py --lookback-days 5

# 2. 本地測試 (Dry-run)：指定本地資料夾且不寄信，產生 HTML 預覽與 Excel 報表
python main_cloud_runner.py --local-dir ./output --no-email
```

### 5.4 批次 Parquet 轉 Excel 工具 (多行程加速)

```bash
python batch_parquet_to_excel.py
```

### 5.5 本地執行尾盤放量站上均價線 (VWAP) 與分點逆向歸因引擎 (隔日沖/波段大戶識別)

```bash
# 1. 預設執行 (自動抓取最新交易日資料，終端輸出繁中報表並匯出 Excel)
python reverse_broker_matcher.py

# 2. 指定分析歷史特定交易日
python reverse_broker_matcher.py --date 2026-09-02

# 3. 自訂過濾門檻 (成交張數 >= 2000 張、均價溢價 1.0%~5.0%、分點淨買超 >= 200 張)
python reverse_broker_matcher.py --min-vol-sheets 2000 --min-vwap-premium 1.0 --min-broker-net-vol 200

# 4. 指定輸出 Excel 檔案路徑
python reverse_broker_matcher.py --output-excel ./output/最新尾盤放量決策表.xlsx
```

### 5.6 本地執行外資與本土法人分點買賣超解密日報

```bash
# 1. 預設執行 (分析最新交易日，產出終端摘要與 Excel 報表)
python institutional_broker_rankings.py

# 2. 產出日報並自動發送 Email 與 Telegram 推播
python institutional_broker_rankings.py --send-email

# 3. 指定歷史特定交易日
python institutional_broker_rankings.py --date 2026-09-02 --send-email
```

### 5.7 myStock 雲端籌碼戰情室資料庫同步 (Supabase)

```bash
# 1. 本地演練模式 (檢驗全市場四週期吸籌、出貨、外資法人與尾盤歸因之聚合 JSON，不寫入資料庫)
python sync_to_mystock.py --dry-run

# 2. 正式同步最新交易日主力情報至 Supabase 雲端資料庫 (驅動 myStock 戰情室)
python sync_to_mystock.py

# 3. 指定歷史特定交易日同步
python sync_to_mystock.py --date 2026-09-02
```

> **注意**：正式同步需在 `.env` 中設定 `SUPABASE_URL` 與 `SUPABASE_KEY`（或使用 `--supabase-url` 與 `--supabase-key` 參數傳遞）。初次設定請先至 Supabase SQL Editor 執行 `supabase_schema.sql` 完成資料表建立。

---

## 6. GitHub Actions 雲端每日自動排程與外部定時觸發

本系統已配置工作流檔案 `.github/workflows/daily_heavy_accumulation_report.yml`：

### 6.1 外部秒級精準排程 (Cron-job.org Webhook 觸發 - 強烈推薦 ⭐)
* **為何改用外部排程**：GitHub 內建排程 (`schedule: cron`) 共享佇列常態性延遲 15~45 分鐘，為確保在每日分點爬蟲產檔完畢後**分秒不差、準時發動主力重押運算**，已將內建排程註解關閉，全面改由外部排程網站發送 Webhook API。
* **觸發端點**：
  ```text
  POST https://api.github.com/repos/ark945/stock_data_analysis/actions/workflows/daily_heavy_accumulation_report.yml/dispatches
  ```
* **詳細設定教學**：請參閱專屬指南 [`外部排程網站觸發GitHub_Actions指南.md`](file:///d:/MyProject/stock_data_analysis/外部排程網站觸發GitHub_Actions指南.md)。
* **建議執行時間**：每週一至週五台灣時間 **20:07** 或 **20:15**（在全市場爬蟲資料上傳 GDrive 之後）。

### 6.2 GitHub 網頁端手動一鍵執行
* 前往 GitHub 專案頁面 ➔ `Actions` ➔ 選擇 `每日台股主力重押日報` ➔ 點擊 `Run workflow`，系統會依序完成：
  1. `main_cloud_runner.py`：5日、10日、20日、60日 四週期主力吸籌雷達全自動掃描並寄發 Email 與 Telegram 通知。
  2. `find_exit_cases.py`：**主力出貨/逃離雷達**（反向指標），直接沿用同一批已下載之 Parquet 快取資料 (`./temp_cache_parquet`)，找出大戶下車案例並寄發第二封 Email。

### 6.3 產出保存 (Artifacts)
* 產出的 HTML 報表與 Excel 檔案將自動保存於 GitHub Actions 的 Artifacts 中（保留 14 天），隨時可於 Actions 頁面下載複盤。

---

## 7. 分析指標定義與報表解讀

### 7.1 主力波段連續重押雷達指標

| 欄位名稱 | 意義說明 | 判斷重點 |
| :--- | :--- | :--- |
| **股票標的** | 個股代號、繁體中文名稱與市場別標記 | 如 `2059 川湖 (上市)`、`6531 愛普* (上櫃)`、`4546 長亨 (興櫃)`、`3644 凌嘉科 (興櫃)` |
| **市場別** | 掛牌交易市場分類 | 標記為 **`上市`**、**`上櫃`** 或 **`興櫃`** |
| **主力券商分點** | 核心重押分點 | 如 `9275 凱基-三多` |
| **主力點火起算日** | 主力首度大額建倉/爆發起算日 | **關鍵核心指標**：由 DuckDB 累計曲線智慧偵測主力「首度突破 20% 總部位」之真實點火日（非單純取分析區間第 1 天），精準還原主力動手起點 |
| **吃貨歷時 (天)** | 從點火起算日至最新活躍日之天數 | 判斷是「剛點火（$\le 7$ 天）」的初升飆股，或是「波段吃貨數十天」的長波大戶 |
| **資料區間起日 / 最新活躍日** | 分析資料檔之統計窗口與最後進出日 | 標註回溯分析之數據區間涵蓋範圍 |
| **進出天數 / 買超天數** | 波段持續性 | 買超天數佔比 $> 70\%$ 代表持續吸籌而非單日沖銷 |
| **累計淨買超 (張)** | 總累積建倉張數 | 換算累積張數 (張) |
| **買進純度佔比 (%)** | 買進量 / (買進+賣出) | $\ge 75\%$ 代表純多單重押鎖碼，非當沖洗量 |
| **主力買進均價 (元)** | 主力平均建倉成本 | 股價回測此均價區通常具備強力防守力道 |
| **淨買超金額 (億元)** | 主力總資金投入 (億元) | 億元級主力重押具備極高指標性與行情發動潛力 |
| **主力吸籌強度評分** | 綜合資金、純度與天數之評分 | 滿分 100 分，分數越高代表主力重押集中度越極端 |
| **吸籌特徵標籤** | 智慧型態標籤 | `🚀 剛點火`、`🔥 連續吸籌`、`🎯 絕對鎖碼`、`💰 億級重押`、`⭐ 川湖重押型` |

### 7.2 尾盤放量站上 VWAP 與分點逆向歸因指標

| 欄位名稱 | 意義說明 | 判斷重點 |
| :--- | :--- | :--- |
| **全日均價 (VWAP)** | 當日市場成交量加權平均價 ($\frac{\text{總成交金額}}{\text{總成交股數}}$) | 當日市場多數參與者的持有成本線，股價在其上代表多方佔優 |
| **均價溢價率 (%)** | $\frac{\text{收盤價} - \text{VWAP}}{\text{VWAP}} \times 100\%$ | 正值代表收在浮盈成本區間；介於 $0.5\% \sim 5\%$ 最佳，過大可能過熱 |
| **尾盤推手分點** | 當日對該標的重押且買均價貼合收盤價之關鍵券商 | 透過均價空間逆向定位還原之尾盤買方主力 |
| **分點買均價** | 該分點當日全部買單之加權平均成交價 | 若高於 VWAP 且貼近收盤價，證實其於高檔/尾盤主動推升吃貨 |
| **均價貼合度 (%)** | $\frac{\|\text{分點買均價} - \text{收盤價}\|}{\text{收盤價}} \times 100\%$ | 數值越小（$< 2.5\%$）代表分點買盤與尾盤高點的重合度越高 |
| **主力屬性標籤** | 主力分類特徵 | `⚡ 隔日沖急拉警報`、`💎 波段主力高位鎖碼`、`🔥 本土實力大戶推升` |
| **次日作戰指引** | 針對該主力屬性量身訂製之隔日盤前作戰指令 | 明確提示「開高不追/逢高停利」或「順勢續抱/回測加碼」 |

---

## 8. 全市場股票名稱與市場別 (上市/上櫃/興櫃) 對照維護

系統採用多來源資料庫整合架構，自動對應全市場 46,000+ 檔標的名稱與市場分類：
1. **資料來源**：
   - 台灣證券交易所 (TWSE) 官方 OpenAPI（上市行情與上市公司基本資料）
   - 證券櫃檯買賣中心 (TPEx) 官方 OpenAPI（上櫃行情與上櫃公司基本資料）
   - 證期局 ISIN 證券編碼系統（以 CP950 解碼上市 Mode 2、上櫃 Mode 4、興櫃 Mode 5）
   - 歷史與特殊股票補正字典（確保 `4546` 長亨、`3644` 凌嘉科 等下市櫃或興櫃標的永久準確）
2. **手動更新指令**：
   ```bash
   python update_stock_mappings.py
   ```
   執行後將自動重新抓取官方最新資料並更新 `stock_name_map.json` 與 `stock_market_map.json`。

---

## 9. 常見問題排解 (FAQ & Troubleshooting)

### Q1: 如何取得 Gmail 的應用程式密碼 (App Password)？
> **解答**：
> 1. 前往 Google 帳戶安全性設定 -> 開啟「兩步驟驗證」。
> 2. 搜尋並進入「應用程式密碼 (App passwords)」。
> 3. 建立一個名稱為 `StockRadar` 的應用程式密碼（16 位英文字母）。
> 4. 將該組密碼填入 `MAIL_PASSWORD`。

### Q2: Google Drive 下載時出現找不到檔案或權限不足？
> **解答**：請確認 Google Cloud 服務帳戶（Service Account）的 Email 已被加入到目標 Google Drive 資料夾的「檢視者」或「共用」名單中，且 `GDRIVE_FOLDER_ID` 設定無誤。

### Q3: 終端輸出的中文顯示為問號或亂碼？
> **解答**：程式已內建 `sys.stdout.reconfigure(encoding="utf-8")`，在 Windows PowerShell 或 CMD 請確認編碼為 UTF-8（可預先執行 `chcp 65001`）。

### Q4: 某些個股或興櫃股票顯示為「未知」？
> **解答**：請執行 `python update_stock_mappings.py` 重新更新全市場快取，或在 `update_stock_mappings.py` 中的 `OVERRIDE_STOCKS` 字典中補充該個股代號與名稱。

---

## 10. 雲端排程與手動觸發雙模式 (GitHub Actions & Supabase)

### 1. 每日排程模式 (Default / 定時觸發)
* **行為**：自動拉取當天最新資料，執行四週期運算。
* **無條件刪除當天舊資料**：在寫入前自動呼叫 `purge_date_from_supabase`，清空 Supabase 中當天 5 張資料表所有舊紀錄，確保全新寫入、絕對不殘留、零重複。
* **派發通知**：正常寄送完整 HTML 報表、4-Sheet Excel 附件至 Email，並發送 Telegram 推播（附帶戰情室連結）。

### 2. 人工點擊模式 (workflow_dispatch / 手動補跑)
* **參數輸入**：
  * `target_date`：可手動輸入欲重跑的交易日（例：`2026-09-03`），若留空則自動處理最新當天。
  * `send_email`：預設為 **False (關閉)**，避免人工補跑或除錯時發信打擾收件人。
* **行為**：
  * 自動無條件清空該指定交易日於 Supabase 的舊資料。
  * 重新計算並塞入全新的籌碼戰情至 myStock 雲端戰情室。
  * 依照設定跳過寄信，純更新雲端戰情室。

---

## 11. 籌碼衍生指標量化分析引擎 (Chip Derivatives & Macro Engine)

本引擎整合「每日券商買賣家數差」、「每日融資券與券資比」、「集保千張大戶持股」與「期交所微觀期貨部位」，產出五合一決策戰報。

### 11.1 核心指標解讀
1. **每日券商買賣家數差 (`broker_diff`)**：
   - `家數差 = 買進券商家數 - 賣出券商家數`
   - **負值極大 (例: -300 家)**：代表數百家散戶分點在賣，只有少數主力分點大買，**籌碼極度集中**。
   - **正值極大 (例: +400 家)**：代表主力出貨倒給全台散戶，**籌碼凌亂發散**。
2. **資券軋空與接刀坑 (`analyze_margin_short_signals`)**：
   - **🚀 極品軋空**：高券資比 (>10%) ＋ 融券單日大增 ＋ 家數差為負 (大戶收籌碼)。
   - **🩸 散戶接刀套牢坑**：融資單日暴增 (>150 張) ＋ 家數差為正 (籌碼擴散給散戶接刀)。
3. **集保大戶基期驗證 (`verify_tdcc_harmony`)**：
   - 自動比對標的千張大戶比例，標註 `👑 超高鎖碼 (>70%)` 或 `🔒 穩健鎖碼 (>50%)`。
4. **大盤微觀期權避震 (`eval_macro_futures_radar`)**：
   - 自動提取外資大台淨口數與散戶小台多空比，警示「高危誘多」或「極品軋空」。

### 11.2 操作指令與歷史回補範例

#### 運算單一交易日 (產出 5-Sheet Excel 戰報)
```bash
python chip_derivatives_engine.py --date 2026-09-02
```

#### 批次回補歷史區間 (配合手邊 2026-06-01 起之分點資料)
```bash
python chip_derivatives_engine.py --start-date 2026-06-01 --end-date 2026-09-03
```
> **輸出檔案**：`./daily_reports/籌碼衍生指標戰報_YYYY-MM-DD.xlsx`，內含 5 大工作表：
> 1. `籌碼極度集中(買賣家數差)`
> 2. `籌碼發散出貨(家數差為正)`
> 3. `極品軋空候選`
> 4. `散戶接刀套牢坑`
> 5. `大盤微觀期權避震`

### 11.3 同步至 Supabase 雲端與 myStock 戰情室

本專案支援將衍生指標、大盤期權避震與資券集保標籤一鍵同步至 Supabase，並在 `myStock` 網頁戰情室即時呈現：
1. **同步指令**：
   ```bash
   # 同步指定交易日
   python sync_to_mystock.py --date 2026-09-03
   
   # 批次同步區間
   python sync_to_mystock.py --start-date 2026-09-01 --end-date 2026-09-03
   ```
2. **myStock 戰情室三大對應視覺**：
   - **大盤微觀期權避震雷達**：頂部多空司令對決卡下方即時呈現外資大台淨OI、散戶小台多空比與情緒警報。
   - **四週期吸籌卡片**：每檔個股右上角自動帶入 `[券資比 XX%]` 與 `[千張大戶 XX%]` 徽章。
   - **第 6 個專屬分頁【📊 衍生與資券雷達】**：
     - **四大夜光膠囊按鈕**：支援【全部衍生情報】、【🚀 極品軋空候選】、【⚠️ 散戶接刀套牢坑】、【💎 籌碼極度集中榜】點擊即時過濾。
     - **指標數值解讀**：
       - `買賣家數差`：青藍色負值（如 `-409 家`）代表籌碼高度流向少數主力分點，集中度極高；橘黃色正值代表籌碼發散倒給散戶。
       - `最新收盤價`：即時比對日 K 收盤價 Parquet。
       - `券資比`：紅字突顯軋空潛能（>10% 配合融券暴增優先篩選）。
       - `千張大戶%`：紫色徽章標示大戶籌碼鎖碼基期 (>70% 為超高鎖碼王者)。

