# 📈 台股券商分點大數據分析與主力吸籌雷達系統 — 操作手冊 (User Manual)

---

## 📌 目錄
1. [系統簡介與核心價值](#1-系統簡介與核心價值)
2. [專案目錄架構與模組清單](#2-專案目錄架構與模組清單)
3. [環境建置與依賴安裝步驟](#3-環境建置與依賴安裝步驟)
4. [資料庫與數據準備](#4-資料庫與數據準備)
5. [操作指令與使用範例](#5-操作指令與使用範例)
   - [5.1 全市場主力波段連續重押吸籌雷達 (核心推薦)](#51-全市場主力波段連續重押吸籌雷達-核心推薦)
   - [5.2 依不同門檻與自訂條件掃描](#52-依不同門檻與自訂條件掃描)
   - [5.3 批次 Parquet 轉 Excel 工具 (多行程加速)](#53-批次-parquet-轉-excel-工具-多行程加速)
   - [5.4 單檔 Parquet 快速轉檔](#54-單檔-parquet-快速轉檔)
6. [分析指標定義與報表解讀](#6-分析指標定義與報表解讀)
7. [常見問題排解 (FAQ & Troubleshooting)](#7-常見問題排解-faq--troubleshooting)

---

## 1. 系統簡介與核心價值

本系統為專門針對台灣證券交易所（TWSE）與櫃買中心（TPEX）全市場千萬筆券商分點交易數據所建構之高敏捷量化分析引擎。

### 🌟 核心特色
1. **極速 DuckDB 分析引擎**：零拷貝直接穿透多天期 Parquet 巨量日報表，數秒內完成全市場統計運算。
2. **川湖（2059）+ 凱基-三多（9275）重押模型複製**：
   - 抓出「單一主力分點連續重押、佔個股成交量超高比重、淨買超金額龐大」之黃金主力飆股。
3. **多行程批次轉檔引擎**：提供高效能 CPU 多核心平行轉換，秒級將 Parquet 大檔轉換為帶格式之 Excel (.xlsx)。
4. **全繁體中文視覺化報表**：自動產出標準化、對齊良好的終端分析報表與 Excel/CSV 匯出檔案。

---

## 2. 專案目錄架構與模組清單

```
stock_data_analysis/
├── find_similar_cases.py          # [核心入口] 全市場主力波段連續吸籌雷達掃描器
├── batch_parquet_to_excel.py      # [工具] 批次多行程 Parquet 轉 Excel 轉檔器
├── parquet_to_excel.py            # [工具] 單檔 Parquet 快速轉檔器
├── stock_name_map.json            # 股票代碼與中文名稱快取
├── 20260822分點資料/               # 歷史/每日全市場分點 Parquet 資料庫
├── output/                        # 分析產出報表 (Excel / CSV) 儲存目錄
├── requirements.txt               # 專案 Python 依賴清單
├── 操作手冊.md                     # 本操作手冊
└── .ai_rules.md                   # AI 協同規則
```

---

## 3. 環境建置與依賴安裝步驟

### 3.1 Python 版本建議
* 建議使用 **Python 3.10 ~ 3.12** (64-bit)。

### 3.2 安裝必要套件
在命令提示字元 (CMD) 或 PowerShell 中執行：

```bash
cd d:\MyProject\stock_data_analysis
pip install -r requirements.txt
```

---

## 4. 資料庫與數據準備

分析模組直接讀取 `20260822分點資料/` 目錄（或自訂資料夾）下之所有 `.parquet` 檔案。
若需取得最新市場資料，可使用 `stock_data_downloader` 專案下載並將產生的 Parquet 檔案放入此目錄。

---

## 5. 操作指令與使用範例

### 5.1 全市場主力波段連續重押吸籌雷達 (核心推薦)

執行全市場掃描，自動分析並按「主力淨買超金額」排序：

```bash
python find_similar_cases.py
```

### 5.2 依不同門檻與自訂條件掃描

```bash
# 1. 調整重押門檻 (買超金額 >= 1,000 萬元，佔比 >= 15%)
python find_similar_cases.py --min-amount 10000000 --min-ratio 0.15

# 2. 指定自訂資料夾路徑與輸出檔名
python find_similar_cases.py --data-dir "20260822分點資料" --output "output/custom_radar_result.xlsx"

# 3. 指定連續買超最少天數
python find_similar_cases.py --min-days 3
```

### 5.3 批次 Parquet 轉 Excel 工具 (多行程加速)

將分點資料庫內所有 Parquet 檔案多核心並行轉換為 Excel：

```bash
python batch_parquet_to_excel.py
```

### 5.4 單檔 Parquet 快速轉檔

```bash
python parquet_to_excel.py "20260822分點資料/TW_Stock_BSR_2026-08-22.parquet"
```

---

## 6. 分析指標定義與報表解讀

雷達報表主要輸出欄位及代表意義：

| 欄位名稱 | 意義說明 | 判斷重點 |
| :--- | :--- | :--- |
| **股票代碼 / 名稱** | 目標標的 | 如 `2059 川湖` |
| **券商分點名稱** | 核心重押分點 | 如 `9275 凱基-三多` |
| **連續買超天數** | 波段持續性 | 天數越長代表波段佈局意圖越明確 |
| **波段淨買超張數** | 總累積建倉張數 | 換算累積張數 (張) |
| **波段淨買超金額** | 主力總資金投入 (萬元) | 億元級主力重押具備極高指標性 |
| **平均買進成本價** | 主力平均建倉價格 | 股價回測此價位通常具備強力支撐 |
| **主力買超佔比** | 佔該股總成交量百分比 | 佔比 > 10%~20% 代表個股籌碼被主力高度鎖定 |

---

## 7. 常見問題排解 (FAQ & Troubleshooting)

### Q1: DuckDB 執行時記憶體不足？
> **解答**：DuckDB 預設採用串流處理（Streaming Query），對記憶體非常節省；若資料量超過 5,000 萬筆，可於腳本內設定記憶體上限參數 `PRAGMA max_memory='4GB'`。

### Q2: 終端輸出的中文顯示為問號或亂碼？
> **解答**：程式已內建 `sys.stdout.reconfigure(encoding="utf-8")`，在 Windows PowerShell 或 CMD 請確認編碼為 UTF-8（可預先執行 `chcp 65001`）。
