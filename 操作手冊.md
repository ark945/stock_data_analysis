# 台股券商分點量化分析系統操作手冊 (User Manual)

本手冊提供台灣證券交易所（TWSE）與櫃買中心（TPEX）全市場千萬筆券商分點資料之量化分析、主力雷達掃描、雲端自動化產製 Email 決策日報與轉檔工具之完整操作說明。

---

## 目錄 (Table of Contents)
1. [系統簡介與核心價值](#1-系統簡介與核心價值)
2. [專案目錄架構與模組清單](#2-專案目錄架構與模組清單)
3. [環境建置與依賴安裝](#3-環境建置與依賴安裝)
4. [組態設定與環境變數 (Config & Settings)](#4-組態設定與環境變數-config--settings)
5. [操作指令與使用範例](#5-操作指令與使用範例)
   - [5.1 本地執行主力波段連續重押雷達](#51-本地執行主力波段連續重押雷達)
   - [5.2 雲端全流程自動化日報 (Google Drive + Email HTML)](#52-雲端全流程自動化日報-google-drive--email-html)
   - [5.3 批次 Parquet 轉 Excel 工具 (多行程加速)](#53-批次-parquet-轉-excel-工具-多行程加速)
6. [GitHub Actions 雲端每日自動排程佈署](#6-github-actions-雲端每日自動排程佈署)
7. [分析指標定義與報表解讀](#7-分析指標定義與報表解讀)
8. [常見問題排解 (FAQ & Troubleshooting)](#8-常見問題排解-faq--troubleshooting)

---

## 1. 系統簡介與核心價值

本系統為專門針對台股全市場千萬筆券商分點交易數據所建構之高敏捷量化分析引擎。

### 🌟 核心特色
1. **極速 DuckDB 分析引擎**：零拷貝直接穿透多天期 Parquet 巨量日報表，數秒內完成全市場統計運算。
2. **川湖（2059）+ 凱基-三多（9275）重押模型複製**：
   - 抓出「單一主力分點連續重押、買進純度超高、淨買超金額龐大」之黃金主力飆股。
3. **無伺服器 (Serverless) 雲端全自動化**：
   - 自動從 Google Drive 目標資料夾拉取最新 Parquet 數據。
   - 每日自動產生 FinTech 響應式 HTML 表格日報並發送至指定 Email 信箱，隨信夾帶 Excel 複盤明細。
4. **多行程批次轉檔引擎**：提供高效能 CPU 多核心平行轉換，秒級將 Parquet 大檔轉換為帶格式之 Excel (.xlsx)。

---

## 2. 專案目錄架構與模組清單

```
stock_data_analysis/
├── docs/
│   └── ai_planning/
│       └── cloud_daily_report_planning.md  # 雲端 Email 自動化架構規劃書
├── .github/
│   └── workflows/
│       └── daily_heavy_accumulation_report.yml # GitHub Actions 每日排程工作流
├── main_cloud_runner.py          # 雲端自動化日報全流程主控入口
├── cloud_gdrive_downloader.py    # Google Drive 近 N 日 Parquet 檔案智慧下載模組
├── cloud_report_generator.py     # 主力重押模型運算、HTML 模板渲染與 Excel 產製模組
├── send_email_report.py          # 安全 SMTP 郵件派發模組 (支援 HTML 與 Excel 附件)
├── find_similar_cases.py         # 本機主力重押雷達掃描工具
├── batch_parquet_to_excel.py     # 多行程 Parquet 批次轉 Excel 工具
├── parquet_to_excel.py           # 單檔 Parquet 快速轉檔工具
├── compare_datasets.py           # 資料庫一致性驗證與比對工具
├── stock_name_map.json           # 全市場股票代號與中文名稱快取
├── requirements.txt              # 專案 Python 套件依賴清單
└── 操作手冊.md                   # 系統完整操作手冊
```

---

## 3. 環境建置與依賴安裝

### 3.1 Python 環境需求
* **Python 版本**：建議使用 Python 3.10 ~ 3.12。

### 3.2 安裝依賴套件
```bash
pip install -r requirements.txt
```

若需使用 Google Drive API 或發送郵件功能，請確保已安裝：
```bash
pip install duckdb pandas numpy openpyxl requests beautifulsoup4 google-api-python-client google-auth
```

---

## 4. 組態設定與環境變數 (Config & Settings)

若要啟用「Google Drive 雲端數據拉取」與「Email 自動寄送」，可在專案根目錄建立 `.env` 檔案或在 GitHub Repository Secrets 中配置：

```ini
# Google Drive 雲端同步
GDRIVE_UPLOAD_URL="https://script.google.com/macros/s/YOUR_GAS_DEPLOYMENT_ID/exec"
GDRIVE_FOLDER_ID="your_google_drive_folder_id"
GDRIVE_LOG_FOLDER_ID="your_google_drive_log_folder_id"

# Telegram 即時推播 (選用)
TELEGRAM_BOT_TOKEN="your_telegram_bot_token"
TELEGRAM_CHAT_ID="your_telegram_chat_id"

# SMTP 郵件通知 (Gmail)
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your_email@gmail.com"
SMTP_PASSWORD="your_gmail_app_password"   # 請使用 Gmail 16 位應用程式專用密碼
RECEIVER_EMAIL="your_email@gmail.com"
```

---

## 5. 操作指令與使用範例

### 5.1 本地執行主力波段連續重押雷達

```bash
# 預設執行 (掃描 output 目錄下所有 Parquet 檔案)
python find_similar_cases.py

# 自訂門檻 (淨買超 >= 0.5 億元, 買進純度 >= 75%, 張數 >= 100 張)
python find_similar_cases.py --min-amount 0.5 --min-ratio 75 --min-vol 100
```

### 5.2 雲端全流程自動化日報 (Google Drive + Email HTML)

```bash
# 1. 完整全自動執行：從 GDrive 下載近 5 日數據 -> 分析 -> 產出 HTML 與 Excel -> 寄送 Email
python main_cloud_runner.py --lookback-days 5

# 2. 本地測試 (Dry-run)：指定本地資料夾且不寄信，產生 HTML 預覽與 Excel 報表
python main_cloud_runner.py --local-dir ./output --no-email
```

### 5.3 批次 Parquet 轉 Excel 工具 (多行程加速)

```bash
python batch_parquet_to_excel.py
```

---

## 6. GitHub Actions 雲端每日自動排程與外部定時觸發

本系統已配置工作流檔案 `.github/workflows/daily_heavy_accumulation_report.yml`：

### 6.1 外部秒級精準排程 (Cron-job.org Webhook 觸發 - 強烈推薦 ⭐)
* **為何改用外部排程**：GitHub 內建排程 (`schedule: cron`) 共享佇列常態性延遲 15~45 分鐘，為確保在每日分點爬蟲產檔完畢後**分秒不差、準時發動主力重押運算**，已將內建排程註解關閉，全面改由外部排程網站發送 Webhook API。
* **觸發端點**：
  ```text
  POST https://api.github.com/repos/ark945/stock_data_analysis/actions/workflows/daily_heavy_accumulation_report.yml/dispatches
  ```
* **詳細設定教學**：請參閱專屬指南 [`外部排程網站觸發GitHub_Actions指南.md`](file:///d:/MyProject/stock_data_analysis/外部排程網站觸發GitHub_Actions指南.md)。
* **建議執行時間**：每週一至週五台灣時間 **20:07** 或 **20:15**（在全市場爬蟲資料上傳 GDrive 之後）。

### 6.2 GitHub 網頁端手動一鍵執行
* 前往 GitHub 專案頁面 ➔ `Actions` ➔ 選擇 `每日台股主力重押日報` ➔ 點擊 `Run workflow`，系統會直接一鍵完成 5日、20日、60日 三週期全自動掃描並寄發 Email 與 Telegram 通知。

### 6.3 產出保存 (Artifacts)
* 產出的 HTML 報表與 Excel 檔案將自動保存於 GitHub Actions 的 Artifacts 中（保留 14 天），隨時可於 Actions 頁面下載複盤。

---

## 7. 分析指標定義與報表解讀

| 欄位名稱 | 意義說明 | 判斷重點 |
| :--- | :--- | :--- |
| **股票標的** | 個股代號與名稱 | 如 `2059 川湖`、`3017 奇鋐` |
| **主力券商分點** | 核心重押分點 | 如 `9275 凱基-三多` |
| **主力點火起算日** | 主力首度大額建倉/爆發起算日 | **關鍵核心指標**：由 DuckDB 累計曲線智慧偵測主力「首度突破 20% 總部位」之真實點火日（非單純取分析區間第 1 天），精準還原主力動手起點 |
| **吃貨歷時 (天)** | 從點火起算日至最新活躍日之天數 | 判斷是「剛點火（$\le 7$ 天）」的初升飆股，或是「波段吃貨數十天」的長波大戶 |
| **資料區間起日 / 最新活躍日** | 分析資料檔之統計窗口與最後進出日 | 標註回溯分析之數據區間涵蓋範圍 |
| **進出天數 / 買超天數** | 波段持續性 | 買超天數佔比 $> 70\%$ 代表持續吸籌而非單日沖銷 |
| **累計淨買超 (張)** | 總累積建倉張數 | 換算累積張數 (張) |
| **買進純度佔比 (%)** | 買進量 / (買進+賣出) | $\ge 75\%$ 代表純多單重押鎖碼，非當沖洗量 |
| **主力買進均價 (元)** | 主力平均建倉成本 | 股價回測此均價區通常具備強力防守力道 |
| **淨買超金額 (億元)** | 主力總資金投入 (億元) | 億元級主力重押具備極高指標性與行情發動潛力 |
| **主力吸籌強度評分** | 綜合資金、純度與天數之評分 | 滿分 100 分，分數越高代表主力重押集中度越極端 |
| **吸籌特徵標籤** | 智慧型態標籤 | `🚀 剛點火`、`🔥 連續吸籌`、`🎯 絕對鎖碼`、`💰 億級重押` |

### 7.1 重要核心參數：主力點火確認門檻比例 (`ignition_threshold_ratio = 0.20`)

本系統拋棄傳統固定金額（如幾百萬元）的粗糙判定方式，採用 **「自適應總部位累積百分比」**：

1. **為何不採用固定金額？**
   * **高價股 vs 低價股**：川湖（一張 1,500 萬）隨便 1 張就破千萬，而 30 元股票 500 萬已是 160 張。固定金額會在高價股抓到散戶單，在低價股抓不到主力單。
   * **資金規模不對稱**：百億超級大戶的 500 萬只是 0.5% 試單雜訊，而千萬級主力的 500 萬是正式重押。
2. **20% 黃金分割點的理論依據**：
   * **主力生命週期**：`[0%~10%] 潛伏試單期` ──> **`[20%~70%] 暴力加碼點火期`** ──> `[70%~100%] 鎖碼拉抬期`。
   * **最佳平衡**：20% 既能完美濾除 6 月份的零星試水溫雜單，又能在主力連續大買的第 2~4 天精準觸發，牢牢鎖定初升段爆發點。
3. **參數自訂指令**：
   * CLI 掃描工具可透過 `--ignition-ratio 0.20` 彈性調整（例如設為 0.15 或 0.25）。

### 7.2 大師級主力選股架構：評分優先、排除 ETF 與 TOP 15 精選

1. **吸籌強度評分優先 (`sort_by = "score"`)**：
   * 傳統純金額排序會被南亞、富邦金、廣達等百億權值牛皮股洗榜。
   * 評分公式大幅加權 **「買進純度（%）」** 與 **「買超天數佔比（%）」**，讓川湖（純度 > 85%、連買 30 天）直接衝上榜首 TOP 3！
2. **自動排除被動 ETF (`exclude_etf = True`)**：
   * 自動過濾 `0050`、`00687B` 等被動資金標的，專注於個股主力的深度波段認養。
3. **HTML 郵件擴大為 TOP 15 精選**：
   * 日報各週期表格展示前 15 大精選標的，兼顧手機版面精煉度與黑馬飆股完整度。

---

## 8. 常見問題排解 (FAQ & Troubleshooting)

### Q1: 如何取得 Gmail 的應用程式密碼 (App Password)？
> **解答**：
> 1. 前往 Google 帳戶安全性設定 -> 開啟「兩步驟驗證」。
> 2. 搜尋並進入「應用程式密碼 (App passwords)」。
> 3. 建立一個名稱為 `StockRadar` 的應用程式密碼（16 位英文字母）。
> 4. 將該組密碼填入 `MAIL_PASSWORD`。

### Q2: Google Drive 下載時出現找不到檔案或權限不足？
> **解答**：請確認 Google Cloud 服務帳戶（Service Account）的 Email 已被加入到目標 Google Drive 資料夾的「檢視者」或「共用」名單中，且 `GDRIVE_FOLDER_ID` 設定無誤。

### Q3: 終端輸出的中文顯示為問號或亂碼？
> **解答**：程式已內建 `sys.stdout.reconfigure(encoding="utf-8")`，在 Windows PowerShell 或 CMD 請確認編碼為 UTF-8（可預先執行 `chcp 65001`）。
